package com.example.repository;

import com.example.model.TurnOver;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TurnOverRepository extends JpaRepository<TurnOver, Long> {
}